import { ProductSortPipe } from './product-sort.pipe';

describe('ProductSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductSortPipe();
    expect(pipe).toBeTruthy();
  });
});
